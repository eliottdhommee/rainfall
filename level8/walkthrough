program makes a gets
we can see 3 arguments we can call
"auth " "service" and "reset" and "login"
then prints %p of "auth " and "service"
if we call login preogram checks if the adress of auth +0x20 is null
if not it calls /bin/sh
auth 
service
service
login
cat /home/user/level9/.pass
